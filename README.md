### Introduction
Not all programm were upload because it represent a lot of code for little variation, there's 8 version of the stat program plus 9 files containing the essential program that permit the good function of each of those version.
Those essential program plus the basic code as been given to me by my tutor.

### Explanation
Histo:
Sort the simulations rather they are single or multiple priority, by printing the name and also gave an histogram of the repartition of those files (initialy design for this).

Stat:
Extract the data (parameter, ID, etc.) of the simulation file, two to be exact the ID of the target star from the feather file and the rest from the hdf5

analyse_efficacite:
analyse the value of the efficiency of each mask, data previously download with stat in a json file

analyse_presence:
give the ratio of apparition of each mask and sky displacement as a function of each parameter, in the python console thought table and save them in graphics file

stat_dlt-mag_dist(2):
calculate the rate of appirition of each product in all simulations

tableaux_metric_priority:
create the heatmap of each parameter as a function of the value of the metric_priority
